# STATUS — 2026-06-02 → 2026-06-03 session

**Repo + Helsinki HEAD `b903c74` on `main` (1:1, in sync).** Bot LIVE, cap-5 canary.
Verified throughout via read-only Helsinki SSH + local code/DB/tests. Operating under
`feedback_ground_truth_only` + framework P1–P5.

## TL;DR
- Found the farmer **kill-switched + idle** (2nd fill-rate kill of the day on news-market adverse fills). **Verified the root mechanism**, applied a **reversible config fix** (`RF_FILL_BREAKER_WINDOW` 180→900), restarted it, and stood up a **30-min Discord watchdog** (`monitor_watchdog.py`, cron).
- Bot ran ~6 h clean, then a **2nd incident** exposed two real bugs (**FX-094 merge broken**, **FX-095 cash-only drawdown false-kill**); the bot **auto-recovered**.
- **Net since restart ≈ −$27** over ~14 h (gross losses ~−$45, reward +$18). **06-02 reward ~$18 (~3× baseline)** — the best rewards-vs-bleed signal yet. Objective net-positive still **UNPROVEN**.
- The unsolved core remains **market selection** (deferred for a designed, owner-approved fix).

## Timeline (UTC)
- **06-02 15:55:40** — fill-rate spike kill (6×>3×) on a news-market adverse-fill burst ("Gemini Pro" market, 4 fills both sides in 28 min). Sticky farmer kill; bot idle. (2nd such kill that day → recurring pattern.)
- **17:30** — after verifying the cause, widened `RF_FILL_BREAKER_WINDOW` 180→900 (config override, backed up) and restarted the farmer. Came up clean (`kill=false`, farming, no new losses).
- **17:40–b903c74** — shipped `monitor_watchdog.py` (read-only, cron `*/30`, Discord on kill/stall/drawdown>12%/desync; alert-only; adversarially tested; fixed a parse bug + a Discord-403 UA bug found in dry-run before arming).
- **18:21 → 22:37** — stable soak: 0 kills, mild lumpy adverse-fill bleed (~−$2/hr), drawdown ~5–6%, single-cycle desyncs proven benign (fill-recording lag, net-zero across cycles).
- **00:00 06-03** — 06-02 reward settled **~$18** ($17.88 + $0.24), ~3× the $5–6 baseline.
- **~03:08–03:27** — Becerra market (`0xa5d79e71`, high-`min_size`, cooldown just expired→redeployed) filled **both sides**: 100 NO @0.277 + 100 YES @0.787 = **$106.40** → bot held a YES+NO pair (~$100 redeemable). The **merge failed** (`'ClobClient' has no 'merge_positions'`) → fell back to lossy dual-dump.
- **~03:30** — **15% drawdown kill fired** — but **cash-only**: the $106 cash→tokens conversion read as 15.3% drawdown (true portfolio ~$1,118 → ~7%). False-ish kill; bot safe-stopped (which incidentally halted the lossy dual-dump).
- **~03:30–07:38** — pair cleared (~$100 cash recovered, positions flat, realized P&L unchanged → redeemed/merged, not dumped at a loss; exact mechanism unconfirmed but cash + `reconcile=ok` confirm no hidden loss). Drawdown dropped <15% → the **oversight drawdown kill auto-cleared** → farming resumed.
- **07:38** — `kill=false`, 5 markets, notional $150, wallet $1,118.31, dd 6.9%, reconcile ok.

## Changes shipped (reversible, single-axis, no branding)
- **Helsinki `config_overrides.json`: `RF_FILL_BREAKER_WINDOW` 180→900.** Verified isolated (only `can_place` reads it; fill-storm detector re-filters its own 300 s window). Revert = 180. Backups `config_overrides.json.bak.pre-breaker-*`.
- **`monitor_watchdog.py`** (`4b94e7b`→`b903c74`). Cron `*/30 * * * * cd /home/polymarket/Polymarket-bot && venv/bin/python3 monitor_watchdog.py >> logs/watchdog.log 2>&1`. Alert-only by design.
- Docs: `docs/SYSTEM_CONTEXT.md` (NEW shareable context), this STATUS, fixit v1.38 amendment.

## New open issues opened this session
- **FX-094 (High)** — `merge_positions` SDK method missing → both-sides fills can't merge to USDC, dual-dump at a loss. (FX-035 class.)
- **FX-095 (High)** — 15% drawdown kill marks **cash-only** not cash+inventory → false-trips when capital converts to inventory.
- **FX-096 (Med)** — FX-084 `unrealized_loss` over-counts (reported $19.1 > $14.40 max possible loss); fail-safe direction but imprecise.
- **FX-097 (Med)** — 24 h cooldown expiry re-deploys known repeat-loser markets (Becerra).
- See `docs/SYSTEM_CONTEXT.md` §4 for full detail + the unsolved market-selection core.

## What's verified working
FX-090 time-to-event filter (timing_excluded 71–86/cycle), FX-092 kill→Discord + FX-074 desync→Discord (both fired on the real kills), FX-051 cooldowns (9+ active overnight), the widened breaker, the new watchdog (cron fired autonomously through the night). Full suite 1118 pass / 2 skip.

## Recommended next work (for owner sign-off — not shipped blind)
1. **FX-094 merge fix** (highest value) — ground-truth the correct V2/CTF merge call so both-sides fills recover ~$0 instead of dual-dumping at a loss.
2. **FX-095 drawdown metric** → cash + inventory (touches a safety limb → sign-off).
3. **Market-selection core** — a fast (30 s) farmer-side adverse-selection guard and/or stability-weighted ranker and/or per-market position-size cap (high-`min_size` markets took $54–$106 bites on a $5–12 canary).
