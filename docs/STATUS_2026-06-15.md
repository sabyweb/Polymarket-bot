# Status Snapshot — 2026-06-15

**As of:** 2026-06-15 ~13:10 UTC · **Prod:** Hetzner Helsinki `hel1` · **HEAD:** `3bfd519`
Supersedes `STATUS_2026-06-13.md`. Point-in-time complement to `CLAUDE.md` / `docs/ONBOARDING_PROMPT.md`.

> Ground truth, not guesswork. Figures below are from read-only probes (bot snapshot / on-chain /
> data-api), reconciled across all three. The live soak is the proof of any change, not a prediction.

## Where the bot is right now
- **Resumed and soaking** (Phase 1 of `docs/PROFITABILITY_PLAN.md`). One process, `NRestarts=0`,
  `kill_switch:false`, cycling normally. Farming **2 markets**, ~$609 resting notional, **0 fills yet**.
- **Portfolio ≈ $1,017.87**, on-chain inventory $0, cash = portfolio. **Drawdown 16.6%** off the
  $1,220.52 peak (a real, recent ~$200 decline over 06-12→06-15 — accepted as baseline, see below).
- **Two levers live (both recorded):**
  - `RF_PREEMPTIVE_COOLDOWN_ENABLED=true` — the **selection fix** being tested: cools a market for
    24h after its first >0.05/share-slippage fill. Addresses the repeat-fill loss bucket (66% of
    14d losses). Currently **armed and idle** (0 preemptive cooldowns — correct, no fills yet).
  - `RF_KILL_DRAWDOWN_FRAC=0.20` (+ `RF_FARMER_DRAWDOWN_KILL_FRAC=0.20`) — **bounded drawdown
    tolerance** so it can run past the real 16.6% drawdown. Kill re-fires at ~$976. **Revert to 0.15
    once portfolio is stably above $1,037** (15% line). Logged in `ground_rules.md` (2026-06-15).
  - Still armed: realized-loss (10%/24h), unrealized-loss (20%), loss-gated fill-rate kill.

## What this session established (so a new reader isn't surprised)
- **Root causes** (see `POSTMORTEM_2026-06-12.md`): RC-1 fill-rate-kill miscalibration (FIXED);
  RC-2 market selection over-weights volatile/news markets (THE core, open); RC-3 price-blind EV gate
  (real but **immaterial** — see below); RC-4 sentinel/null end_date defeats the 48h filter (FIX-1
  built, default-off); RC-5 loss/drawdown metric blind to held-to-resolution losses (FIX-3 built +
  enabled).
- **FIX-2 was dropped.** We proved the deploy count is capped by `MAX_DEPLOYED_MARKETS=5` (a
  deliberate canary bound), not by the EV gate — 243 markets already qualify for 5 slots. So fixing
  the EV price wouldn't add deploys. The `positive_ev=5` that RC-3 rested on was a loop-short-circuit
  artifact.
- **The real problem is selection (which 5), not breadth (how many).** The 5 it picks are top-by-
  reward, which ignores adverse-fill risk → often the worst by net. The cooldown soak is the first
  fix; the cap stays at 5 until net-positive is proven, then scales (Phase 3).
- **The economics:** reward on running days is $9–20/day; losses are concentrated + avoidable
  (top ~15 markets ≈ $150 of $213 over 14d, mostly repeat-fill volatile/news markets). Profitability
  is a credible target *if* selection cuts the avoidable losses — measured by soak, not predicted.

## Immediate next steps
1. **Soak the cooldown** — watch for the first adverse fill (cooldown should fire: `preemptive:`
   entry in `market_cooldowns`), the loss curve bending, and drawdown climbing toward $1,037.
2. **Revert the drawdown override to 0.15** once stably above $1,037.
3. **If it re-kills at ~$976** → cooldown alone insufficient → add the volatility lever (Phase 1.3),
   single-axis.
4. **After a clean 1–2 week non-crisis soak proves net-positive** → raise the cap step-wise (Phase 3).

## Recent commits (local `main`, pushed)
- `3bfd519` drawdown kill cfg-overridable + cooldown-soak resume record + profitability plan.
- Prior: FIX-1 (event guard, default-off), FIX-3 (on-chain kill value), postmortem RC-1..RC-5,
  onboarding manual rewrite.

## Uncommitted / housekeeping
- Untracked `.bak` files on Helsinki (harmless). Untracked audit artifacts on the Mac.
- The daily soak-progress check (drawdown trend / cooldown-fired / per-market net / revert flag) is
  proposed but not yet wired as a scheduled task.
