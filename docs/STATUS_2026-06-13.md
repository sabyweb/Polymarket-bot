# Polymarket Reward-Farming Bot — Status Snapshot

**As of:** 2026-06-13 ~03:35 UTC · **Prod:** Hetzner Helsinki `hel1`
**Audience:** first-time reader (human or agent). Point-in-time complement to the always-true
index in `CLAUDE.md`, the full manual in `docs/ONBOARDING_PROMPT.md`, the contract in
`ground_rules.md`, and the root-cause ledger in `docs/POSTMORTEM_2026-06-12.md`.

> **Prime directive: ground truth, not guesswork.** Every figure below is from a read-only
> probe against the live Helsinki DB / data-api during this session. Aggregates may have moved
> slightly since; conclusions don't depend on the last dollar.

---

## 1. Current state (verified ~03:35 UTC)

- **Farming again, kill cleared.** A manual `systemctl restart polymarket-farmer` at ~02:43 UTC
  brought up a fresh process (cycle reset to ~1), `kill_switch: false`, **no re-kill since**.
  Currently **1 active market**, ~$23.76 live notional.
- **All three core units `active` + `enabled`** (farmer, oversight, dashboard).
- **Wallet ~$1,047.84**, peak **$1,220.52**, drawdown **~14%** (under the 15% kill line).
- `global_tighten` has dropped back to **False** (the 24h loss/reward ratio recovered).
- **24h total loss: −$72.58** (authoritative — portfolio total_value $1,120.42 → $1,047.84,
  confirmed to the cent by the data-api net cash flow). Gross position loss ≈ **−$83** (SELL
  $412 + REDEEM $200 + MERGE $3.6 − BUY $699) offset by **+$10.38** rewards/rebates. ⚠ The
  `unwinds` table shows only **−$7.66** (active dumps); the rest is positions held to resolution
  and **redeemed at a loss** (chiefly the SpaceX IPO suite) — which `unwinds` does not record, so
  the bot's `realized_loss_24h` metric ($13.54 earlier) materially **undercounts** the true loss.
  Intraday trough $907.47 (−$213 from base) before redemptions recovered it.
- Allocator this cycle: `eligible≈3,425 → positive_ev=5 → deploys=5` (the RC-3 bottleneck:
  ~99.7% of eligible markets fail the price-blind EV gate). **31 markets in cooldown**, incl. 3
  `chronic_blocked: manual clear required`.
- A small **benign wallet desync** (+$3.62, wallet *up*, zero trade deltas) appeared off the
  settlement hour — almost certainly a late reward/rebate credit; not the draining kind. Watch
  it re-baseline; no action.

---

## 2. What happened in the prior ~12 hours

- **~14:00 UTC 06-12:** a fill burst (13:35–13:47, ~11 fills) on the **SpaceX IPO** market-cap
  markets adversely filled the bot. The **loss-gated fill-rate kill fired correctly**
  (`fill_rate_ratio=6.0 > 3.0×` **+** `1h_loss=$7.64 > $5.42` gate). Working as designed — not a
  false fire.
- **~14:00 → ~02:43 (06-13):** **~12h sticky-killed and idle** (0 markets, no orders, no bleed —
  capital protected). The watchdog paged every 30 min throughout.
- **~02:43 06-13:** operator-authorized restart cleared the sticky kill (cause reviewed first,
  per the cardinal rule); farming resumed.
- **Loss attribution (24h, true −$72.58):** almost entirely **one market —
  "SpaceX IPO closing market cap above $2.4T?" = −$88** (bought ~$88 of YES at ~0.22, **held to
  resolution, redeemed $0** — it resolved worthless). Everything else roughly netted out:
  `$2.2T` +$9.99, `Up/Down First Day` +$5.84, `$2T` −$10.37 (after dumping), rewards +$6.71.
  These are **RC-4** (sentinel/null `end_date` let the allocator farm a same-day-resolving event
  market). **Critically, the −$88 was invisible to `unwinds` and to every loss-based kill** — it
  crystallized at resolution, not via a dump — which is why `realized_loss_24h` read only $13.54
  and no safety signal fired on the day's biggest loss. → candidate **RC-5** (loss metric blind
  to redemption/resolution losses).

---

## 3. Work completed this session (2026-06-12/13)

- **RC-1 fixed (live):** loss-gated fill-rate kill; severity-tiered alerting (**Telegram** +
  dedicated Discord critical channel + `@here`); watchdog re-pages while killed, alerts on
  sustained alive-but-idle, and pings **Healthchecks.io** as a dead-man's-switch; all units
  `enabled` for reboot survival.
- **Diagnosed + proved two new root causes** against live `compute()` and the activity ledger:
  **RC-3** (price-blind EV gate over-rejection) and **RC-4** (sentinel/null `end_date` →
  SpaceX IPO trades).
- **Postmortem upgraded to the root-cause ledger + locked fix plan** (`docs/POSTMORTEM_2026-06-12.md`
  §11). Committed locally as `0e99da1` (not pushed).
- **FIX-1 built** (RC-4): `RF_ALLOC_EVENT_DATE_GUARD` (default-OFF) in `config.py` +
  `simple_allocator.py`; 5 new unit tests. **Gate green** — `run_audit_v5` PASS (6/6 scenarios,
  INV3/5/7) and `tests/test_simple_allocator.py` 44 passed / 1 skipped. **Commit + trial pending.**
- **Onboarding manual rewritten** (`docs/ONBOARDING_PROMPT.md`) and this status written.

---

## 4. Open work (priority order)

1. **Commit FIX-1** (clean message, no AI branding), then run the disciplined trial: deploy
   (inert — flag off), flip `RF_ALLOC_EVENT_DATE_GUARD=true` on the canary, watch
   `event_excluded=` in `[OVERCOMMIT_ALLOC]` for ≥7 days — confirm it catches IPO-type markets
   **without** collapsing eligible/deploy counts.
2. **FIX-2 — real-price enrichment for the EV gate (RC-3).** The bigger lever (deploys ~5 →
   potentially many); only after FIX-1 closes the near-resolution leak. Single-axis: populate
   `midpoint_guess` from a real price before `_est_cost_per_market`.
3. **RC-2 (market selection)** — keep `reward_snapshot.py` accumulating per-market reward; once
   ~1–2 weeks of data exist, rank by true **net** (reward − loss) and trial one cut (cooldown or
   cap) via Wave-4. Cut net-bad, keep net-good.
4. **Operational:** review the 3 `chronic_blocked` markets (incl. a ~$87/day-reward one); clear
   by hand only after confirming they no longer adverse-fill.

---

## 5. Repo state

- **Local `main`:** `0e99da1` (postmortem) committed, **1 ahead of `origin/main`, not pushed.**
  FIX-1 changes (`config.py`, `simple_allocator.py`, `tests/test_simple_allocator.py`) staged-
  ready, **not yet committed**.
- **Uncommitted docs:** `docs/ONBOARDING_PROMPT.md` (rewritten), `docs/STATUS_2026-06-13.md`
  (this file). Older `docs/STATUS_2026-06-10.md` superseded by this snapshot.
- **Untracked clutter** (cosmetic): `audit_*.json`, `audit_v5_out*/`, `*.db-wal/-shm`, paper DBs
  — candidates for `.gitignore` sometime.

---

## 6. The one-line summary

Bot is **farming again and safe** after a correct ~12h protective kill (SpaceX-IPO adverse
fills). The kill was right; the *cause* (RC-4 sentinel-date markets) is now understood and
**FIX-1 is built and gated, awaiting commit + a 7-day canary trial**. The deeper bottleneck
(RC-3 price-blind EV gate → only 5 deploys) is next as FIX-2. Net-positive remains the unproven
objective; market selection is still the whole game.
